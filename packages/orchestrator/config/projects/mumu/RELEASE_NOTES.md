# Mumu Manifest Notes

## Phase 2 extract state contract

B-5's `extract_template_from_draft` prompt emits `payload.chatter.extract_state`.
C-1 and C-4 must parse that field and preserve the prompt-side state machine contract.
